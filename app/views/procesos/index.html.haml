.wrapper_with_padding
  %h3 Procesos de selección
  %buttom= link_to "New Precess", new_proceso_path, class: "button"

  #notes.clearfix
    - unless @procesos.blank?
      - @procesos.each do |proceso|
        %a{href:(url_for [proceso])}
          .note
            %h2.title= proceso.name
            %p.title= proceso.state
            
    - else
      %h2 Add a note
      %p It appears you haven't created any notes yet... Lets fix that. Why don't you create a new note below
      %buttom= link_to "New Precess", new_proceso_path
